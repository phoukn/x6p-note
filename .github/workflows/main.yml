name: 检查 Markdown 语法

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]
  workflow_dispatch:  # 手动触发测试用

jobs:
  lint-markdown:
    runs-on: ubuntu-latest

    steps:
      - name: 下载仓库
        uses: actions/checkout@v4

      - name: 安装 markdownlint（Node.js 工具）
        run: |
          npm install -g markdownlint-cli

      - name: 检查 README.md 语法
        run: markdownlint README.md || echo "有 Markdown 语法警告（常见小问题，比如标题空格、列表缩进），不影响阅读，继续玩机！"

      - name: 鼓励一下！
        run: |
          echo "MD 检查完毕！Root 稳了～ 😎"
          echo "LA 时间现在是 $(TZ=America/Los_Angeles date '+%Y-%m-%d %H:%M')"
          echo "笔记继续记，hosts 继续屏蔽，吃饭继续浪！🍔"
